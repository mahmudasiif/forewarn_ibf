/** Model runs — routing shell only. Real work belongs in src/features/. */
export default function ModelRunsPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold">Model runs</h1>
      <p className="mt-2 text-sm opacity-70">features/models — run history, inputs, outputs, timing</p>
    </div>
  );
}
