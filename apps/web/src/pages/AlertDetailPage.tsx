/** Alert detail — routing shell only. Real work belongs in src/features/. */
export default function AlertDetailPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold">Alert detail</h1>
      <p className="mt-2 text-sm opacity-70">features/alerts — content, recipients, delivery log</p>
    </div>
  );
}
