/** Models — routing shell only. Real work belongs in src/features/. */
export default function ModelsPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold">Models</h1>
      <p className="mt-2 text-sm opacity-70">features/models — registry, health, versions</p>
    </div>
  );
}
