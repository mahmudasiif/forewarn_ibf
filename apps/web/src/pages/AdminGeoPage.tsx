/** Geographies — routing shell only. Real work belongs in src/features/. */
export default function AdminGeoPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold">Geographies</h1>
      <p className="mt-2 text-sm opacity-70">features/admin — admin units and exposure layers</p>
    </div>
  );
}
