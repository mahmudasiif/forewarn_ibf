/** Impact matrix — routing shell only. Real work belongs in src/features/. */
export default function ImpactMatrixPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold">Impact matrix</h1>
      <p className="mt-2 text-sm opacity-70">features/impact — hazard x severity x exposure editor</p>
    </div>
  );
}
