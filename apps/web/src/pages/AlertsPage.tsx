/** Alerts — routing shell only. Real work belongs in src/features/. */
export default function AlertsPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold">Alerts</h1>
      <p className="mt-2 text-sm opacity-70">features/alerts — issued alerts and their status</p>
    </div>
  );
}
