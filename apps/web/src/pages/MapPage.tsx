/** Map — routing shell only. Real work belongs in src/features/. */
export default function MapPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold">Map</h1>
      <p className="mt-2 text-sm opacity-70">features/map — full-screen Leaflet with hazard/impact/exposure layers</p>
    </div>
  );
}
