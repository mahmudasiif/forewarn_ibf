/** Settings — routing shell only. Real work belongs in src/features/. */
export default function AdminSettingsPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold">Settings</h1>
      <p className="mt-2 text-sm opacity-70">features/admin</p>
    </div>
  );
}
