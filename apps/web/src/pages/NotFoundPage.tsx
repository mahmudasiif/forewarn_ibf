/** Page not found — routing shell only. Real work belongs in src/features/. */
export default function NotFoundPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold">Page not found</h1>
      <p className="mt-2 text-sm opacity-70">404</p>
    </div>
  );
}
