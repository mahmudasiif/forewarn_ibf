/** Forecasts — routing shell only. Real work belongs in src/features/. */
export default function ForecastsPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold">Forecasts</h1>
      <p className="mt-2 text-sm opacity-70">features/hazards — forecast browser by issue/valid time</p>
    </div>
  );
}
