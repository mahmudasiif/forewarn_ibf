/** Reports — routing shell only. Real work belongs in src/features/. */
export default function ReportsPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold">Reports</h1>
      <p className="mt-2 text-sm opacity-70">features/reports — generated reports and exports</p>
    </div>
  );
}
