/** Hazards — routing shell only. Real work belongs in src/features/. */
export default function HazardsPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold">Hazards</h1>
      <p className="mt-2 text-sm opacity-70">features/hazards — list of hazard events</p>
    </div>
  );
}
