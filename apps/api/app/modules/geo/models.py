"""Geo ORM models. Tables live in the `geo` Postgres schema."""
from app.core.db import Base

SCHEMA = "geo"

# class Example(Base):
#     __tablename__ = "example"
#     __table_args__ = {"schema": SCHEMA}
