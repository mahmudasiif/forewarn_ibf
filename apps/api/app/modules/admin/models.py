"""Admin ORM models. Tables live in the `admin` Postgres schema."""
from app.core.db import Base

SCHEMA = "auth"

# class Example(Base):
#     __tablename__ = "example"
#     __table_args__ = {"schema": SCHEMA}
