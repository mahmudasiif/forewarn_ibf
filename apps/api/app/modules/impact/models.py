"""Impact ORM models. Tables live in the `impact` Postgres schema."""
from app.core.db import Base

SCHEMA = "impact"

# class Example(Base):
#     __tablename__ = "example"
#     __table_args__ = {"schema": SCHEMA}
