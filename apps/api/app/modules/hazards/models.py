"""Hazards ORM models. Tables live in the `hazards` Postgres schema."""
from app.core.db import Base

SCHEMA = "hazard"

# class Example(Base):
#     __tablename__ = "example"
#     __table_args__ = {"schema": SCHEMA}
