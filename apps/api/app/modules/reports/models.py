"""Reports ORM models. Tables live in the `reports` Postgres schema."""
from app.core.db import Base

SCHEMA = "content"

# class Example(Base):
#     __tablename__ = "example"
#     __table_args__ = {"schema": SCHEMA}
