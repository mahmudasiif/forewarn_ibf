"""Alerts ORM models. Tables live in the `alerts` Postgres schema."""
from app.core.db import Base

SCHEMA = "alerts"

# class Example(Base):
#     __tablename__ = "example"
#     __table_args__ = {"schema": SCHEMA}
