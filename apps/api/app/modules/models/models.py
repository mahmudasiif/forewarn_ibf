"""Models ORM models. Tables live in the `models` Postgres schema."""
from app.core.db import Base

SCHEMA = "models"

# class Example(Base):
#     __tablename__ = "example"
#     __table_args__ = {"schema": SCHEMA}
